---
- hosts: kubemaster1
  tasks:
    - name: Change the SystemdCgroup value to true
      lineinfile:
        path: /home/ajaufura/test.toml
        line: "SystemdCgroup = true"
        state: present
        create: yes
        insertafter: EOF
        regexp: '^SystemdCgroup\s*='
